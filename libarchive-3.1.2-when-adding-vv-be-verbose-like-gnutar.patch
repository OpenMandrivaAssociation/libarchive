diff -Naur libarchive-3.1.2/tar/read.c libarchive-3.1.2.tpg/tar/read.c
--- libarchive-3.1.2/tar/read.c	2013-01-14 01:43:45.000000000 +0000
+++ libarchive-3.1.2.tpg/tar/read.c	2015-01-28 20:24:59.581875861 +0000
@@ -323,10 +323,16 @@
 			 * deferred '\n'.
 			 */
 			if (bsdtar->verbose) {
+			    if (bsdtar->verbose < 2)  {
 				safe_fprintf(stderr, "x %s",
 				    archive_entry_pathname(entry));
 				fflush(stderr);
-			}
+			} else {
+			/* Print more verbose (vv) messages to STDOUT */
+				list_item_verbose(bsdtar, stdout, entry);
+                    		fflush(stdout);
+                    	    }
+                	}
 
 			/* TODO siginfo_printinfo(bsdtar, 0); */
 
@@ -344,8 +350,13 @@
 					fprintf(stderr, "\n");
 				bsdtar->return_value = 1;
 			}
-			if (bsdtar->verbose)
-				fprintf(stderr, "\n");
+			if (bsdtar->verbose) {
+			    if (bsdtar->verbose > 1) {
+				    fprintf(stdout, "\n");
+			    } else {
+				    fprintf(stderr, "\n");
+			    }
+			}
 			if (r == ARCHIVE_FATAL)
 				break;
 		}
